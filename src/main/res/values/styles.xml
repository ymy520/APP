<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <!--修改在应用打开状态下，的一些系统的配置（电量、时间）-->
        <item name="colorPrimaryDark">@color/whole_baseColor</item>
       <!-- <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <!--底部导航菜单的单选按钮的样式-->
    <style name="bottom_menu_rb">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:paddingTop">3dp</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@drawable/textcolor</item>
        <item name="android:textSize">@dimen/whole_firstTextSize</item>
    </style>

    <!--以下开始是商家页面相关参数-->
    <!--spinner的样式-->
    <style name="style_spinner">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:dropDownVerticalOffset">40dp</item>
        <item name="android:dropDownWidth">1000dp</item>
    </style>
    <!--定义spinner之间的线的样式-->
    <style name="style_mian_verline">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">25dp</item>
        <item name="android:background">#dcdcdc</item>
        <item name="android:layout_gravity">center_vertical</item>
    </style>

    <style name="style_horizontal_divise">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:background">@color/whole_divider</item>
    </style>
</resources>
